---
import BaseLayout from "../../../layouts/BaseLayout.astro";
import { getProjects } from "../../../lib/cms/projects";
import type { Project } from "../../../lib/cms/projects";
import Section from "../../../components/layout/Section.astro";

export async function getStaticPaths() {
  const projects = await getProjects();

  return projects.map((project) => ({
    params: { slug: project.slug },
    props: { project },
  }));
}

interface Props {
  project: Project;
}

const { project } = Astro.props;
const base = import.meta.env.BASE_URL.replace(/\/?$/, "/");

const labels = {
  allProjects: "← Wszystkie projekty",
  technicalHighlights: "Najważniejsze aspekty techniczne",
  problem: "Problem",
  myRole: "Moja rola",
  architecture: "Architektura",
  hardestProblem: "Najtrudniejszy problem techniczny",
  lessonsLearned: "Wnioski",
  viewOnGitHub: "Zobacz na GitHub",
  liveDemo: "Demo na żywo",
};

const allProjectsHref = `${base}pl/projects`;

const proseSections = [
  { heading: labels.problem, content: project.problem },
  { heading: labels.myRole, content: project.myRole },
  { heading: labels.architecture, content: project.architecture },
  { heading: labels.hardestProblem, content: project.hardestProblem },
  { heading: labels.lessonsLearned, content: project.lessonsLearned },
].filter((s) => s.content);
---

<BaseLayout
  title={`${project.title} | Pawel Ryfiak`}
  description={project.summary}
  lang="pl"
  path={`/projects/${project.slug}`}
>
  <main id="main-content">
    <Section>
      <a class="link link-primary text-sm" href={allProjectsHref}>{labels.allProjects}</a>

      <div class="mt-6 flex flex-wrap gap-2">
        {project.categories.map((cat) => (
          <span class="badge badge-primary">{cat}</span>
        ))}
      </div>

      <h1 class="mt-4 text-4xl font-bold tracking-tight">{project.title}</h1>
      <p class="mt-4 max-w-3xl text-lg text-base-content/70">{project.summary}</p>

      <!-- Tech stack -->
      <div class="mt-5 flex flex-wrap gap-2">
        {project.stack.map((item) => (
          <span class="badge badge-outline">{item}</span>
        ))}
      </div>

      <!-- Technical highlights -->
      {
        project.highlights.length > 0 && (
          <div class="mt-10 rounded-xl border border-base-300 bg-base-200 p-6">
            <h2 class="text-lg font-semibold">{labels.technicalHighlights}</h2>
            <ul class="mt-4 space-y-2">
              {project.highlights.map((point) => (
                <li class="flex items-start gap-3 text-base-content/75">
                  <span class="mt-0.5 text-primary" aria-hidden="true">▸</span>
                  {point}
                </li>
              ))}
            </ul>
          </div>
        )
      }

      <!-- Case study sections -->
      {
        proseSections.length > 0 && (
          <div class="mt-10 space-y-10 max-w-3xl">
            {proseSections.map(({ heading, content }) => (
              <div>
                <h2 class="text-xl font-semibold">{heading}</h2>
                <p class="mt-3 leading-relaxed text-base-content/75">{content}</p>
              </div>
            ))}
          </div>
        )
      }

      <!-- Links -->
      <div class="mt-12 flex flex-wrap gap-3">
        {
          project.githubUrl && (
            <a
              href={project.githubUrl}
              class="btn btn-outline"
              target="_blank"
              rel="noopener noreferrer"
            >
              {labels.viewOnGitHub}
            </a>
          )
        }
        {
          project.liveUrl && (
            <a
              href={project.liveUrl}
              class="btn btn-primary"
              target="_blank"
              rel="noopener noreferrer"
            >
              {labels.liveDemo}
            </a>
          )
        }
        <a href={allProjectsHref} class="btn btn-ghost">{labels.allProjects}</a>
      </div>
    </Section>
  </main>
</BaseLayout>


