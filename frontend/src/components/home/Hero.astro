---
import ParticlesBackground from "../ui/ParticlesBackground.svelte";
import { base } from "../../lib/url";
interface Props {
  lang?: "en" | "pl";
}
const { lang = "en" } = Astro.props;

const copy = {
  en: {
    available: "Available for opportunities",
    subtitle: "Junior Software Engineer focused on backend, automation, and practical DevOps.",
    body: "I build Spring Boot systems, deployment pipelines, AI-assisted data extraction tools, and infrastructure scripts. Currently learning Astro, Svelte, Strapi, and modern frontend workflows by building this portfolio.",
    openTo: "Open to: IT internship, junior backend, automation, DevOps-oriented roles · Antwerp / remote / EU",
    viewProjects: "View projects",
    downloadCv: "Download CV",
  },
  pl: {
    available: "OTWARTY NA STAŻE I ROLE JUNIORSKIE",
    subtitle: "Student Applied Computer Science — backend Java/Spring, automatyzacja i podstawy deploymentu.",
    body: "Tworzę aplikacje backendowe w Spring Boot, pracuję z bazami danych, REST API, Dockerem i Linuksem oraz buduję narzędzia automatyzujące powtarzalne procesy. Aktualnie uczę się Astro, Svelte, Strapi i nowoczesnego frontendu, budując to portfolio.",
    openTo: "Szukam: stażu programistycznego, roli junior backend lub zadań związanych z automatyzacją · Antwerpia / okolice Głogowa / zdalnie / UE",
    viewProjects: "Zobacz projekty",
    downloadCv: "Pobierz CV",
  },
};
const c = copy[lang];
---

<section class="relative overflow-hidden py-20 md:py-32">
  <ParticlesBackground client:load />
  <div class="relative z-10 mx-auto max-w-[1120px] px-4">
    <div class="max-w-3xl" data-reveal>
      <p class="mb-4 text-sm font-medium uppercase tracking-widest text-primary">
        {c.available}
      </p>

      <h1 class="text-4xl font-bold leading-tight tracking-tight md:text-6xl">
        Paweł Ryfiak
      </h1>

      <p class="mt-4 text-xl font-medium text-base-content/80 md:text-2xl">
        {c.subtitle}
      </p>

      <p class="mt-6 max-w-2xl text-base leading-relaxed text-base-content/65">
        {c.body}
      </p>

      <p class="mt-3 text-sm text-base-content/50">
        {c.openTo}
      </p>

      <div class="mt-8 flex flex-wrap gap-3">
        <a
          href={lang === "pl" ? `${base}pl/projects` : `${base}projects`}
          class="btn btn-primary"
        >
          {c.viewProjects}
        </a>
        <button type="button" class="btn btn-outline" data-cv-trigger>
          {c.downloadCv}
        </button>
      </div>
    </div>
  </div>
</section>
